
0526.cpp

# 깊이
- 가려진 면을 제거  
- 픽셀이 그려지면 깊이 값이라는 값을 할당하여 관측자로 부터의 위치를 결정  
- 화면상의 같은 위치에 다른 픽셀이 그려질때 깊이 값을 비교  
- 낮은 깊이 값을 가진 픽셀은 화면에 안 보임  

if (bDepth)  
	glEnable(GL_DEPTH_TEST);  
else  
	glDisable(GL_DEPTH_TEST);  

이 모드를 사용하기 위해서는 depth관련 기능을 enable해줘야 함  

glClear(GL_COLOR_BUFFER_BIT | GL_DEPTH_BUFFER_BIT);  
glutSwapBuffers();  
glutInitDisplayMode(GLUT_DOUBLE | GLUT_RGB | GLUT_DEPTH);  

